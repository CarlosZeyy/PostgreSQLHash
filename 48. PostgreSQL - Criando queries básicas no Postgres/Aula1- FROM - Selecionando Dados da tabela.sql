SELECT * FROM products;

SELECT product_id, product_name FROM products;

SELECT 
	product_id,
	product_name,
	unit_price,
	units_in_stock
FROM products; -- Se não tiver um ';' fechando o codigo e logo abaixo eu tiver outro codigo vai dar erro.


-- SELECT 
-- 	employee_id, first_name, last_name, city
-- FROM employees;